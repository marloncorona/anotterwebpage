@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager

@{
    ViewBag.Title = "Colaborar";
    Layout = "_Layout";
}
@* 
@if (!SignInManager.IsSignedIn(User))
{
    <div style="margin-top:3rem; display:flex; flex-direction:column; align-items:center; text-align:center;">
        <h2 style="font-weight:600; margin-bottom:1rem;">Inicia sesión para colaborar</h2>

        <p style="max-width:400px; color:#555; margin-bottom:2rem;">
            Necesitas una cuenta para acceder al formato de colaboración.
        </p>

        <div style="display:flex; gap:1rem;">
            <a class="btn-contact" asp-area="Identity" asp-page="/Account/Login">Iniciar sesión</a>

            <a style="border: none;" class="btn-contact" asp-area="Identity" asp-page="/Account/Register">Crear cuenta</a>
        </div>
        <img src="~/images/ottieSneaking.png" style="width:60%;height: auto; margin-top:1.5rem; justify-content: center;" />
    </div>

    return;
} *@

<img class="login-bg" src="~/images/accountBg.png" alt="Fondo" />

<h2 class="mainTitle">Colaboraciones</h2>

<div class="collab-container">
    <form class="collab-form">
        <h3 class="collab-title">Cuéntame sobre ti</h3>

        <input type="text" name="nombre" placeholder="Nombre" required class="collab-input" />

        <input type="text" name="apellido" placeholder="Apellido" required class="collab-input" />

        <input type="text" name="nombreOrg" placeholder="Nombre de la institución (opcional)" class="collab-input" />
        <small class="collab-hint">*Si no aplica, puedes dejarlo en blanco.</small>

        <input type="email" name="email" placeholder="Email" required class="collab-input" />

        <input type="text" name="numero" placeholder="Whatsapp" required class="collab-input" />

        <textarea name="motivo" placeholder="Motivo de colaboración" class="collab-textarea"></textarea>

        <div class="collab-actions">
            <button type="submit" class="collab-btn">Enviar</button>
        </div>
    </form>
    <img src="~/images/ottieColab.PNG" />
</div>
